(function () {
  // Backend base URL — Frank's Orby instance, exposed via Cloudflare tunnel.
  // SWAP THIS when the tunnel rotates or you move Orby to a persistent host.
  window.AURORA_DELI_API_BASE = 'https://shopper-juice-viii-drainage.trycloudflare.com';
})();
